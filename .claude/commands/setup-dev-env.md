To start the development environment 

Prerequsite 
Must be logged into an openshift cluster. 
Check with the command line argument `oc whoami`. If it outputs "Unable to connect to the server:" then you are not logged in, inform the user they must be logged into an openshift cluster to proceed. 

Setup Operators 
Review 
1. Install Loki Operator 
2. Install OpenShift Logging Operator 

